[package]
name = "automata-protocol-protobuf-codegen"
version = "0.0.0"
edition = "2024"
publish = false
license = "MIT"

[dependencies]
prost-build = { version = "=0.14.4", default-features = false, features = ["format"] }
protox = { version = "=0.9.1", default-features = false }

# This manifest is a template. The verification script copies it under the
# repository's target/agent-scratch directory before invoking Cargo.
[workspace]
