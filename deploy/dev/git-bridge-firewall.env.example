# Exact private smart-Git listener protected by automata_git_bridge_guard.
# Copy to a nonsymlink config path and adjust both values for the host.
AUTOMATA_GIT_BRIDGE_LISTEN_ADDRESS=192.168.0.8
AUTOMATA_GIT_BRIDGE_LISTEN_PORT=8088
